[ ]: permission only in the current directory not in ~ and not in /usr/bin and not everywhere
[ ]: permission only in the current directory and subdirectories
[ ]: this tool should be a binary and being call like that : ```mcp-agent-kit-rs new-feature "add an endpoint /users/{id}/preferences"```