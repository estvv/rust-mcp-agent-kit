[package]
name = "mcp-agent"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "mcp-agent"
path = "src/main.rs"

[dependencies]
mcp-client = { path = "../mcp-client" }
clap = { version = "4", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"