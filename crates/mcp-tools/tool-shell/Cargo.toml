[package]
name = "tool-shell"
version = "0.1.0"
edition = "2021"

[dependencies]
mcp-core = { path = "../../mcp-core" }
serde_json = "1.0"
inventory = "0.3"
tokio = { version = "1", features = ["rt", "process", "time"] }
libc = "0.2"

[target.'cfg(unix)'.dependencies]
libc = "0.2"